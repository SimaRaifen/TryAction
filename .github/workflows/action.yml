on: [push]

jobs:
  send_message_job:
    runs-on: ubuntu-latest
    name: send message to slack
    steps:
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
